extends ../layout

block content
    div(class="container")
        h2(class="mt-5")
            i(class="fas fa-stack me-2")
            .
                Région
        form(method="post" action="/region/update/"+region[0]._id enctype="multipart/form-data")
            div(class="mb-3 row")
                .col-12
                    label(class="form-label") titre
                .col-12
                    input(class="form-control" name="title" required="true" value=region[0].title)
            div(class="mb-3 row")
                .col-12
                    label(class="form-label") description
                .col-12
                    textarea(class="form-control" name="description" required="true")= region[0].description
            div(id="input-container" count= region[0].gallery.length )
                div(id="img-container" class="d-flex ms-sm-5 my-3 flex-wrap justify-content-center justify-content-sm-start")
                        for image , key in region[0].gallery
                            div(class="mx-2 my-1 position-relative")
                                img(src='/images/locations/gallery/' + image style="width:180px;height:180px;object-fit:cover;")
                                div(class="delete btn btn-danger position-absolute top-0 end-0 rounded-0" region=region[0]._id pos=image) x
                a(id="add-line" class="btn btn-outline-success mb-3 mt-3") Ajouter une image
            boutton(class="btn btn-primary mb-5") sauvegarder
